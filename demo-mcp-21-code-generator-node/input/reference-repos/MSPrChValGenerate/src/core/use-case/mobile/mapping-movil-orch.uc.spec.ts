import { IMappingMovilOrchUC } from "./mapping-movil-orch.uc";

describe('ConsultStoredProcedure', () => {
    it('should have the correct values on ConsultStoredProcedure', () => {
      expect((IMappingMovilOrchUC)).toBeDefined();
    });
});