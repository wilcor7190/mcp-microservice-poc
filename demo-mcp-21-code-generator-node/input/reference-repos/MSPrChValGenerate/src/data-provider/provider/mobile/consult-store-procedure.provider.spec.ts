import { IConsultStoredProcedureProvider } from "./consult-store-procedure.provider";

describe('ConsultStoredProcedure', () => {
    it('should have the correct values on ConsultStoredProcedure', () => {
      expect((IConsultStoredProcedureProvider)).toBeDefined();
    });
  
  
  });